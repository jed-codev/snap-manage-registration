// Create a test for the resolvers using Jest
import { resolvers } from "../src/resolvers";

// Test the resolvers
describe("Example Scenario", () => {
  it("should be true", async () => {
    expect(true).toBeTruthy;
  });
});
